# Fixtures — datawedge-intent-output
- `scan-broadcast.json` → a simulated DataWedge broadcast intent with extras
  (`com.symbol.datawedge.data_string` = `"0123456789"`, `com.symbol.datawedge.label_type` = `LABEL-TYPE-CODE128`).

*(TODO: add the fixture payload.)*
