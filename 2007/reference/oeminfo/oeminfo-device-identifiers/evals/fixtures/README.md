# Fixtures — oeminfo-device-identifiers
Device-backed or mock-provider values.

- `build_serial` → expected non-empty string matching the device serial format.

*(TODO: add a mock ContentProvider fixture so this runs in CI without a device.)*
