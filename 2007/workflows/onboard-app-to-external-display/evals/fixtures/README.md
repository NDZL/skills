# Fixtures — onboard-app-to-external-display
- `managed-config.json` → must validate against the WSC managed-config schema.

Note: full external-display rendering is a **device / manual** gate; config-validate + compile are automatable in CI.
