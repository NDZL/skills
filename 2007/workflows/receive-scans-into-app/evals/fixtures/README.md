# Fixtures — receive-scans-into-app
- `scan-broadcast.json` → simulated DataWedge broadcast; assert the app receives `data_string` = `"0123456789"`.

*(TODO: add the fixture payload + confirm the sample's profile config.)*
